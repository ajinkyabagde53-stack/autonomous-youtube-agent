# Research Inputs

Add local research artifacts here while live source adapters are being built.

Recommended folders:

- `youtube/`
- `reddit/`
- `search/`
- `transcripts/`

Supported formats in the current foundation are Markdown and plain text.
The research layer normalizes them into structured ResearchItem objects.
